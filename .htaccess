# LRMS — root .htaccess
# Forwards every request into /public (the only web-exposed directory).

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Block direct browser access to application internals.
    RewriteRule ^(app|config|database|resources|routes|storage|vendor|tests|docs|android|\.github)(/|$) - [F,L]

    # Send everything else to the public front controller directory.
    RewriteRule ^$ public/ [L]
    RewriteRule ^(?!public/)(.*)$ public/$1 [L]
</IfModule>

# Never serve dotfiles, dependency manifests, SQL dumps or logs.
<FilesMatch "^(\.|composer\.(json|lock)|.*\.(sql|log|md|yml|yaml))$">
    Require all denied
</FilesMatch>

<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "DENY"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

Options -Indexes
DirectoryIndex index.php index.html
