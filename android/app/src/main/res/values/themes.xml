<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Compose supplies the real theme; this styles the window around it.

        The parent must stay an AppCompat theme. MainActivity is an AppCompatActivity so
        that the language switch reaches it below Android 13, and an AppCompatActivity
        throws at startup under a framework theme. It was android:Theme.Material before,
        which is why the switch could not be fixed without touching this file too.
    -->
    <style name="Theme.LRMS" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_dark</item>
        <item name="android:navigationBarColor">@color/brand_dark</item>
        <item name="android:windowBackground">#F4F6FA</item>
    </style>

    <!--
        The launch window. Android draws this before the process is ready, so the
        D2 mark appears immediately instead of a blank navy rectangle.

        The splash attributes are API 31+; older versions ignore them and simply
        show windowBackground, which is the same black, so nothing looks broken at
        minSdk 24 — it just does not animate the mark.

        Black rather than the brand navy: the emblem is photographic and carries its
        own black field, so on navy it would read as a box pasted onto the screen.
    -->
    <style name="Theme.LRMS.Launch" parent="Theme.LRMS">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/ic_splash_logo</item>
    </style>
</resources>
