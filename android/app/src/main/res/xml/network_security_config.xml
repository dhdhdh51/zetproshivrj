<?xml version="1.0" encoding="utf-8"?>
<!--
    Customer financial data must not travel in the clear.

    HTTPS is required everywhere except the two loopback addresses used when a
    developer runs the PHP server on their own machine (10.0.2.2 is the emulator's
    view of the host). Release and staging builds additionally set
    usesCleartextTraffic="false" in the manifest.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
